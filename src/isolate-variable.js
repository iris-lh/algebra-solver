module.exports = (input)=> {
  var equation = input[0]
  var target   = input[1]
  console.log(`        isolating variable '${target}' in  ${JSON.stringify(equation)}\n`)
  var newEquation = equation
  var output = newEquation
  return output
}
